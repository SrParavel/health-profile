import { useEffect } from "react";
import type { StepProps } from "../../types/StepProps";

function GeneralHealth(props: StepProps) {
  const { formData, handleChange } = props;

  return <p>General Information</p>;
}

export default GeneralHealth;
